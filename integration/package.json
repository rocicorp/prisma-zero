{
  "name": "integration-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate && pnpm migrate",
    "migrate": "pnpm prisma migrate diff --from-empty --to-schema schema.prisma --script > generated/migration.sql",
    "pretest": "pnpm generate",
    "test": "pnpm build"
  },
  "dependencies": {
    "prisma-zero": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "prisma": "^7.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  },
  "prettier": "@rocicorp/prettier-config"
}
